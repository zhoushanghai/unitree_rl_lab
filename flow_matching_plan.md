# Implementation Plan: Voxel Transition Flow Matching

## 1. 目标描述 (Goal Description)
利用机器人本体的感觉数据 (proprioceptive states) 和上一时刻的体素地图 (Voxel Map)，通过流匹配 (Flow Matching) 模型来预测当前时刻的体素地图。
*   **输入 (Source/Initial State $x_0$)**: $t-1$ 时刻的体素 `collision_voxel`，形状 `(20, 20, 15)`。
*   **输出 (Target State $x_1$)**: $t$ 时刻的体素 `collision_voxel`，形状 `(20, 20, 15)`。
*   **条件变量 (Condition $c$)**: 过去 50 帧 (时间步 $t-49$ 到 $t$) 的机器人状态序列，形状为 `(50, 93)` (共 4650 维)。单帧 93 维仅包含**纯本体感知信息**：角速度 (3) + 重力投影 (IMU姿态) (3) + 关节位置 (29) + 关节速度 (29) + 上一步动作 (29)。*(注：严格不使用线速度等特权信息，强迫 1D-Conv 自行从 50 帧历史中估计运动状态)*

这是一个**基于成对数据 (Paired Data) 的条件流匹配**问题（类似于 Image-to-Image translation 中的 Rectified Flow）。

## 2. 数学表述与模型设计 (Mathematical Formulation)
与从纯噪声出发的标准流匹配不同，你的设定中起点和终点都是具有确定物理意义的分布。由于我们有自然配对的数据 $(V_{t-1}, V_t)$，我们可以直接构建确定性的线性轨迹 (Linear Path)。

*   **状态插值 (Probability Path)**: 
    对于流时间 $s \in [0, 1]$ (注意这里的 $s$ 是流匹配的虚拟时间，区别于真实时间 $t$)：
    $x_s = (1-s) V_{t-1} + s V_t$
*   **目标向量场 (Target Vector Field)**: 
    对 $s$ 求导，得到目标的流动速度：
    $u_s = \frac{d}{ds} x_s = V_t - V_{t-1}$
*   **模型目标 (Objective)**: 
    训练一个神经网络 $v_\theta(x_s, s, c)$ 来拟合这个速度：
    $\mathcal{L}(\theta) = \mathbb{E}_{t, s \sim U(0,1)} \left[ W(x) \cdot \left\| v_\theta(x_s, s, c) - (V_t - V_{t-1}) \right\|^2 \right]$

### 2.1 损失函数设计：基于距离变换的惩罚 (Distance-based Penalty)
由于体素地图极度稀疏，为了严格抑制**远距离假阳性 (False Positives in free space)**（即在空旷区域凭空预测出障碍物，这会导致机器人误触发避障或急刹车），我们需要引入**基于距离变换的空间权重 $W(x)$**。
*   **计算距离场**：在数据预处理阶段或 DataLoader 中，对真实体素 $V_t$ 计算 3D 欧氏距离变换 (Euclidean Distance Transform, EDT)。得到一个距离矩阵 $D_t(x)$，表示空间中每个点到最近障碍物的距离。
*   **权重分配**：
    *   对于真实障碍物及其紧邻区域，距离 $D_t \approx 0$，保持基础权重。
    *   对于距离障碍物越远的“纯空气”区域，距离 $D_t$ 越大，我们赋予其指数级或线性递增的乘法惩罚权重（例如 $W(x) = 1 + \alpha \cdot D_t(x)$）。
    *   当模型在这些远距离空白区域输出不为 0 的虚假流速时，它将面临极其沉重的 Loss 惩罚。

## 3. 网络架构考量 (Architecture Considerations)
因为处理的是 3D 体素数据，建议采用 **3D U-Net** 或 **3D Transformer (如 DiT-3D)** 架构。
*   **主输入**: $x_s$，形状为 `(1, 20, 20, 15)` 的 3D 张量。
*   **条件注入**:
    *   **流时间 $s$**: 通过正弦位置编码 (Sinusoidal Positional Encoding) 嵌入，并通过 MLP 处理。
    *   **状态条件 $c$**: 采用 **3 层 1D 卷积 (1D Conv × 3)**，**卷积核大小设置为 5**。
        *   输入：`(Batch, 93, 50)` 的时序信号 (通道数 93，序列长度 50)。
        *   经过 3 层 1D 卷积（可配合 stride 降采样）后，将特征展平 (Flatten)，再通过一层全连接映射到固定维度 (**512 维**)，作为表征历史运动意图的全局特征向量。
    *   二者相加或拼接后，通过 **FiLM (Feature-wise Linear Modulation)** 层或 **AdaIN** 注入到 3D 卷积的各个残差块中。

## 4. 训练与推理流程 (Training & Inference)
### 4.1 训练阶段
1. 从数据集中随机采样时刻 $t$。
2. 获取 $V_{t-1}$, $V_t$ 以及过去 50 帧的条件序列 $c_{t-49:t}$。
    * **条件输入增强**: 对 $c_{t-49:t}$ 添加微小的随机高斯噪声 $\epsilon \sim \mathcal{N}(0, 0.1^2)$。
    * **源体素增强 (缓解自回归误差)**: 为防止推理阶段误差累积 (Exposure Bias)，向起点 $V_{t-1}$ 中加入适量噪声得到 $\tilde{V}_{t-1}$，使网络学会在生成 $V_t$ 时兼具**纠错(Denoising)**的能力。起点更新为 $x_0 = \tilde{V}_{t-1}$。
3. 从均匀分布 $U[0, 1]$ 中采样 $s$。
4. 计算 $x_s = (1-s) V_{t-1} + s V_t$。
5. 将 $x_s, s, c_t$ 输入模型，计算输出 $v_\theta$。
6. 使用 MSE Loss 计算 $v_\theta$ 与 $V_t - V_{t-1}$ 的误差并反向传播更新参数。

### 4.2 推理阶段 (生成)
1. 给出 $t-1$ 时刻的真实体素 $V_{t-1}$（或者上一步生成的体素）和当前过去 50 帧的条件序列 $c_{t-49:t}$。
2. 令起点 $x_0 = V_{t-1}$。
3. 使用 ODE 求解器 (例如 Euler 法或 Heun 法) 在 $s \in [0, 1]$ 上积分：
   $x_{s+\Delta s} = x_s + \Delta s \cdot v_\theta(x_s, s, c_t)$
   例如取 10 步 ( $\Delta s = 0.1$ )，最终得到的 $x_1$ 即为预测的 $V_t$。
4. 由于输出是连续值，而体素可能是二值的 (0或1)，最后可以通过设定阈值 (如 0.5) 将 $x_1$ 二值化。

## 5. 讨论与开放性问题 (Discussion & Open Questions)

- **为什么要用流匹配而不是直接回归 (Direct Regression)？**
  如果直接用一个网络 $V_t = f(V_{t-1}, c_t)$ 预测，由于只有 1 步变化，模型可能会倾向于输出模糊的均值 (Blurry average)。而流匹配可以通过 ODE 积分过程，保留和生成更锐利的几何边缘，而且在遇到不确定的视野盲区时，能够建模多种可能性的分布。

- **残差学习视角**
  在极短时间间隔内，体素变化很小，即 $V_t \approx V_{t-1}$。模型的学习目标 $V_t - V_{t-1}$ 其实非常稀疏。这有利于网络快速收敛。

### 需要进一步确认的问题：
1. **体素的离散性**：`collision_voxel` 里的数据是连续概率值（如占用概率 $[0,1]$）还是严格的离散二值（0和1）？如果全是二值，流匹配在插值 $x_s$ 时会导致大量中间状态在 $(0, 1)$ 之间，模型需要能从这些中间态恢复出清晰的二值边界。
2. **自回归误差累积**：在实际部署时，你会输入真实的 $V_{t-1}$ 还是模型自己上一步预测出的 $\hat{V}_{t-1}$？如果是后者，误差可能会在多步之后跑偏。
3. **是否需要引入少量噪声**：标准的 Rectified Flow 是一条直线插值。但在生成二值体素时，有时会在轨迹中间加入少量的随机扰动 (Stochasticity) 以增强模型的鲁棒性。你希望从纯确定性 ODE 开始实验吗？
